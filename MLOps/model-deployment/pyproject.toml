[project]
name = "mnist-front"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.35",
    "streamlit-drawable-canvas>=0.9",
    "tensorflow>=2.16",
    "pillow>=10.0",
    "numpy>=1.26",
]

[dependency-groups]
dev = ["pytest>=8.0"]
optional = ["watchdog>=4.0"]
