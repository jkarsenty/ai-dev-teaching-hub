[project]
name = "mnist-front"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.35",
    "streamlit-drawable-canvas>=0.9",
    "pillow>=10.0",
    "requests>=2.31",
]

[dependency-groups]
dev = ["pytest>=8.0"]
optional = ["watchdog>=4.0"]
